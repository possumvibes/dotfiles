# technically all of this gets sourced from bash2env.

# status is-login || exit

# Tools
# set -xg SXHKD_SHELL '/usr/bin/sh'

# Editor


# for editor in kak nvim vim vi micro nano
#     if command -q $editor
# 		set -x EDITOR $editor
# 		set -x VISUAL $editor
#         break
#     end
# end

# # Convenience Directories
# set -x DOTS_DIR "$HOME/.local/share/chezmoi"
# set -x FSHDIR $XDG_CONFIG_HOME/fish
# set -x NOTEBOOK_DIR "$HOME/ari-common/ari_yelling"
# set -x WALLPAPER_DIR "$HOME/Pictures/wallpapers"


# # XDG Compliance
# set -x XDG_CONFIG_HOME $HOME/.config

# set -x STARSHIP_CONFIG $XDG_CONFIG_HOME/starship/starship.toml
# set -x VIMDIR $XDG_CONFIG_HOME/nvim

